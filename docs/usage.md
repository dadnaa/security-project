# Usage

## CLI
```bash
lab run downgrade
lab status
lab logs
```

## Web UI
```bash
python -m lab.cli serve --host 127.0.0.1 --port 5000
```
Then open http://127.0.0.1:5000

## Safety
This is a simulation only. Do not use against real networks.
